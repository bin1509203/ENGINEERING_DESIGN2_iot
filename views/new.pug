doctype html
html
    head
        meta(charset='utf-8')
    body
        h1 
            a(href='/device') Server Side JavaScript
        ul
            each topic in topics
                li
                    a(href="/device/"+topic)= topic
        article        
            form(action="/device" method='post')
                p
                    input(type="text" name='name' placeholder='name')
                p
                    input(type="text" name='power' placeholder='power')
                p
                    input(type="submit")
                    -// 동떨어진 글쓰기 페이지를 main과 유사하게 변경